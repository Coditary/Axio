// RUN: %axc %s -o %tbin
// RUN: %tbin; test "$?" = "17"

enum ActorState as Flags {
    IsAlive,
    Team as Flag {
        Neutral,
        Red,
        Blue,
    },
    IsVisible,
}

fn main() int {
    let state ActorState = ActorState{IsAlive, Team.Blue}
    return state
}
