// RUN: %axc %s -o %tbin
// RUN: %tbin; test "$?" = "4"

package main

enum DistanceBand(label int, mult int) {
    Near(1, 1),
    Mid(2, 2),
    Far(3, 4),
}

fn main() int {
    return DistanceBand.Far.mult
}

