// RUN: %axc %s -o %tbin
// RUN: %tbin; test "$?" = "0"

class Obj {
    value int
}

fn consume(x *Obj) void {
    return
}

fn main() int {
    let unique *Obj = *Obj()
    consume(unique)
    return 0
}
