// RUN: %axc %s -o %tbin
// RUN: %tbin; test "$?" = "7"

package main

class User {
    age int

    fn getAge() int {
        return self.age
    }
}

fn main() int {
    let user = User(7)
    return user.getAge()
}

