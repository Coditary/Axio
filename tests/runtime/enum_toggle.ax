// RUN: %axc %s -o %tbin
// RUN: %tbin; test "$?" = "0"

package main

enum ActorState as Flags {
    IsAlive,
    IsVisible,
}

fn main() int {
    let state ActorState = ActorState{IsVisible}
    return state toggle ActorState.IsVisible
}

