// RUN: %axc --emit-llvm-only %s -o %t 2>/dev/null
// RUN: %filecheck %s < %t.ll

package main

fn main() int {
    let a int = 7 & 3
    let b int = a | 8
    if b >= 3 {
        return b ^ 1
    }
    return 0
}

// CHECK: and
// CHECK: or
// CHECK: icmp sge
// CHECK: xor

