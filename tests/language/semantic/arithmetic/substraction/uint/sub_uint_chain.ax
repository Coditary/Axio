// RUN: not %axc --check-only %s 2>&1 | %filecheck %s

package main

fn main() {
    let a uint = 5
    let b uint = 3
    let c uint = 1
    a - b - c
}

// CHECK: error: initializer type does not match declared type
