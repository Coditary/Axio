// RUN: %axc --dump-ast %s | %filecheck %s

package main

#define BUILD_MODE dev

fn main() int {
    let data str = $readfile("../examples/assets/banner.txt")
    let dialect str = %%SQL{
SELECT * FROM users
}%%
    return 0
}

// CHECK: Preprocessor #define BUILD_MODE dev
// CHECK: CompileCall readfile
// CHECK: Dialect SQL

