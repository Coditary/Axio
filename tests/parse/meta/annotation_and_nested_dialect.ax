// RUN: %axc --dump-ast %s | %filecheck %s

package main

#define BUILD_MODE dev
@ThreadSafe
fn main() int {
    let sql str = %%SQL{
SELECT id FROM users WHERE active = 1
}%%
    let banner str = $readfile("../examples/assets/banner.txt")
    return 0
}

// CHECK: Preprocessor #define BUILD_MODE dev
// CHECK: Annotation @ThreadSafe
// CHECK: Function main
// CHECK: Dialect SQL
// CHECK: CompileCall readfile

