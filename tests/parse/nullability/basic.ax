// RUN: %axc --dump-ast %s | %filecheck %s

package main

fn main() int {
    let ptr Obj = null
    if ptr? {
        ptr?.call()
    }
    return 0
}

// CHECK: Let ptr:Obj
// CHECK: Unary
// CHECK: Call

