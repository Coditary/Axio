// RUN: not %axc --check-only %s 2>&1 | %filecheck %s

package main

class Obj {
}

fn main(src ref Obj) int {
    let copy Obj = src
    return 0
}

// CHECK: error: ref values cannot be stored in owning or value bindings

