// RUN: not %axc --check-only %s 2>&1 | %filecheck %s

fn main() int {
    let raw int = 7
    let value weak int = raw
    return 0
}

// CHECK: error: weak values must originate from ARC or weak references
