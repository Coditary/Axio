// RUN: not %axc --check-only %s 2>&1 | %filecheck %s

class Obj {
}

fn clone(x *Obj) (*Obj, *Obj) {
    return x, x
}

// CHECK: error: unique values cannot be returned more than once from the same statement
