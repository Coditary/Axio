// RUN: %axc --check-only %s 2>&1 | %filecheck %s

fn log{level int}(value int) int {
    return value
}

fn main() int {
    return log{3}(1)
}

// CHECK: warning: compile-time call arguments are parsed but not yet specialized in codegen
