// RUN: not %axc --check-only %s 2>&1 | %filecheck %s

fn pair() (int, int) {
    return 1, 2
}

fn main() int {
    if pair() {
        return 1
    }
    return 0
}

// CHECK: error: if conditions must be single values
