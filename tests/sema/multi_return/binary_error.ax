// RUN: not %axc --check-only %s 2>&1 | %filecheck %s

fn pair() (int, int) {
    return 1, 2
}

fn main() int {
    return pair() + 1
}

// CHECK: error: binary operators require single-value operands
