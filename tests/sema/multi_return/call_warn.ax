// RUN: %axc --check-only %s 2>&1 | %filecheck %s

package main

fn pair(x int) (int, int) {
    return x, x + 1
}

fn main() int {
    let a int, b int = pair(1)
    return a + b
}

// CHECK-NOT: warning: multi-return calls are parsed but tuple/value lowering is not implemented yet

