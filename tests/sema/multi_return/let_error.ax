// RUN: not %axc --check-only %s 2>&1 | %filecheck %s

package main

fn pair() (int, int) {
    return 1, 2
}

fn main() int {
    let value = pair()
    return 0
}

// CHECK: error: multi-return calls cannot initialize a single binding

