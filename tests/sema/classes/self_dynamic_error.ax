// RUN: not %axc --check-only %s 2>&1 | %filecheck %s

package main

class User {
    name str
    print str => print + name
}

// CHECK: error: dynamic class fields cannot directly reference themselves

