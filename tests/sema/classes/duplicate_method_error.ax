// RUN: not %axc --check-only %s 2>&1 | %filecheck %s

package main

class User {
    fn greet() int {
        return 1
    }

    fn greet() int {
        return 2
    }
}

// CHECK: error: duplicate class method 'greet'

