// RUN: not %axc --check-only %s 2>&1 | %filecheck %s

package main

class User {
    greet int

    fn greet() int {
        return 1
    }
}

fn main() int {
    let user = User(0)
    return user.greet()
}

// CHECK: error: class member and method share the name 'greet'

