// RUN: %axc --check-only %s 2>&1 | %filecheck %s

package main

class Box {
    value int
}

fn main() int {
    let ptr Box = new Box()
    if !ptr? {
        return 0
    } else {
        return ptr.value
    }
}

// CHECK-NOT: warning: member access may dereference a nullable value

