// RUN: %axc --check-only %s 2>&1 | %filecheck %s

package main

class User {
    fn greet() int {
        return 1
    }
}

fn main() int {
    let user = User()
    user?.greet()
    return 0
}

// CHECK: warning: null-safe call used on a value that is not known to be nullable

