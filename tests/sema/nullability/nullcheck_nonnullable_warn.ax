// RUN: %axc --check-only %s 2>&1 | %filecheck %s

package main

fn main() int {
    let value int = 3
    if value? {
        return 1
    }
    return 0
}

// CHECK: warning: null-check postfix '?' used on a value that is not known to be nullable

