// RUN: %axc --emit-llvm-only %s -o %t 2>/dev/null
// RUN: %filecheck %s < %t.ll

class User {
    age int

    fn getAge() int {
        return self.age
    }
}

fn main() int {
    let user = User(7)
    return user.getAge()
}

// CHECK: define i32 @User.getAge
// CHECK: call i32 @User.getAge
