// RUN: %axc --emit-llvm-only %s -o %t 2>/dev/null
// RUN: %filecheck %s < %t.ll

class Obj {
    value int
}

fn read(x ref Obj) int {
    return x.value
}

fn main() int {
    let value = Obj(7)
    return read(&value)
}

// CHECK: define i32 @read(ptr %x)
