// RUN: %axc --emit-llvm-only %s -o %t 2>/dev/null
// RUN: %filecheck %s < %t.ll

package main

fn main() int {
    if 5 in 1..=10 {
        return 1
    }
    return 0
}

// CHECK: define i32 @main()
// CHECK: br i1

