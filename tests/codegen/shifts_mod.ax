// RUN: %axc --emit-llvm-only %s -o %t 2>/dev/null
// RUN: %filecheck %s < %t.ll

package main

fn main() int {
    let x int = 9
    let y int = (x << 2) >> 1
    let z int = y % 3
    return z
}

// CHECK: shl
// CHECK: ashr
// CHECK: srem

